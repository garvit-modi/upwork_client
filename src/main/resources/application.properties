# quarkus.datasource.reactive.url=postgresql://localhost:5432/blog_db

quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/blog_db
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=postgres
quarkus.datasource.password=root
quarkus.hibernate-orm.database.generation=update
# quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.http.port=8123
smallrye.jwt.sign.key.location= privatekey.pem
# quarkus.hibernate-orm.active=true

quarkus.log.min-level=DEBUG
quarkus.log.category."io.quarkus.oidc".level=DEBUG

# quarkus.mutiny.smallrye-operators-buffer-size=128




# Enable the RolesAllowedFilter
# quarkus.resteasy.roles.allowed.filter.enabled=true

quarkus.smallrye-jwt.enabled=true


mp.jwt.verify.issuer=jwt-token

# JWT public key (verify the token's signature)
mp.jwt.verify.publickey.location = publickey.pem

# Optional: Specify the audience(s) to validate against
# mp.jwt.verify.audience=my-audience

# for jwt expiration duration
# quarkus.swagger-ui.path=/ui
#quarkus.http.cors=false
# quarkus.debug=true
# quarkus.swagger-ui.path=ui


# Local
%local.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/blog_db
%local.quarkus.datasource.db-kind=postgresql
%local.quarkus.datasource.username=postgres
%local.quarkus.datasource.password=root
%local.quarkus.http.port=8123
